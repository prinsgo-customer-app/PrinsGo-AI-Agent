1. **Workspace fetching**: Modify `mobile-app-native/src/screens/DashboardScreen.tsx` so `fetchWorkspaceAndStatus` first calls GET `/api/ai-agent/workspaces/current` using `api.get`. If it succeeds and returns a workspace, use its ID. If not, call GET `/api/ai-agent/workspaces` and use the first workspace's ID. Update the Zustand store using `setWorkspaceId`. Check file to verify.
2. **DashboardScreen UI Update**: Modify `DashboardScreen.tsx` to read `status.memory` and `status.files` from the GET status response (verified in backend `aiSystemController.js`) instead of hardcoding "NOT_CONFIGURED (Mobile UI)". Add a button "Open Advanced Settings" at the bottom that navigates to the Settings tab. Check file to verify.
3. **AppNavigator Updates**: Modify `mobile-app-native/src/navigation/AppNavigator.tsx` to change the `Settings` tab to use a `SettingsStack`. The Stack will have `SettingsList` (the main settings screen), `Memory`, `Files`, `Approvals`, `GitHub`, `Hermes`, and `Providers`. Check file to verify.
4. **SettingsScreen Updates**: Update `mobile-app-native/src/screens/SettingsScreen.tsx` to keep the profile info and Logout button, but add new touchable elements in a section that call `navigation.navigate('Memory')`, `navigation.navigate('Files')`, `navigation.navigate('Approvals')`, `navigation.navigate('GitHub')`, `navigation.navigate('Hermes')`, and `navigation.navigate('Providers')`. Check file to verify.
5. **Implement Memory Screen**: Write `mobile-app-native/src/screens/MemoryScreen.tsx` to call GET `/api/ai-agent/workspaces/:workspaceId/memory` and display memories. Verify by reading file.
6. **Implement Files Screen**: Write `mobile-app-native/src/screens/FilesScreen.tsx` to call GET `/api/ai-agent/workspaces/:workspaceId/files` and display files. Verify by reading file.
7. **Implement Approvals Screen**: Write `mobile-app-native/src/screens/ApprovalsScreen.tsx` to call GET `/api/ai-agent/workspaces/:workspaceId/approvals` and list them, with buttons to POST `/api/ai-agent/workspaces/:workspaceId/approvals/:approvalId/resolve` with body `{ status: 'APPROVED', reason: 'Mobile approval' }` or `{ status: 'REJECTED', reason: 'Mobile rejection' }` (verified payload in `aiTaskController.js`). Verify by reading file.
8. **Implement GitHub Screen**: Write `mobile-app-native/src/screens/GitHubScreen.tsx` to call GET `/api/ai-agent/workspaces/:workspaceId/github/status`. Verify by reading file.
9. **Implement Hermes Screen**: Write `mobile-app-native/src/screens/HermesScreen.tsx` to call GET `/api/ai-agent/workspaces/:workspaceId/hermes/status`. Verify by reading file.
10. **Implement Providers Screen**: Write `mobile-app-native/src/screens/ProvidersScreen.tsx` to call GET `/api/ai-agent/workspaces/:workspaceId/providers`. Verify by reading file.
11. **AgentsScreen Update**: Update `mobile-app-native/src/screens/AgentsScreen.tsx`. Keep existing GET. Add a text input (for name) and button to call POST `/api/ai-agent/workspaces/:workspaceId/agents` with body `{ name: "New Agent", description: "Mobile Agent", systemInstructions: "Be helpful", provider: "hermes", model: "default", tools: [], permissions: [] }` (verified payload in `aiAgentController.js`) to create a basic agent. Add a toggle button on each agent card to call PATCH `/api/ai-agent/workspaces/:workspaceId/agents/:agentId/toggle` with body `{ isEnabled: !item.isEnabled }` (verified payload in `aiAgentController.js`). Update the list on success. Check file to verify.
12. **TasksScreen Update**: Update `mobile-app-native/src/screens/TasksScreen.tsx`. Keep the GET. Add a text input and button at the top to call POST `/api/ai-agent/workspaces/:workspaceId/tasks` with body `{ agentId: null, instructions: "...", requiresApproval: false, requiredPermissions: [] }` (verified payload in `aiTaskController.js`). Add a button on each task where `status === 'QUEUED'` to call POST `/api/ai-agent/workspaces/:workspaceId/tasks/:taskId/execute` and show the result. Check file to verify.
13. **ChatScreen Update**: Modify `mobile-app-native/src/screens/ChatScreen.tsx`. Ensure it successfully creates tasks with POST `/api/ai-agent/workspaces/:workspaceId/tasks` with body `{ agentId, instructions: userMessage.text, requiresApproval: false, requiredPermissions: [] }` and then executes it with POST `/api/ai-agent/workspaces/:workspaceId/tasks/:taskId/execute`. Use `execRes.data.data.result` (the updated task returned from execution, as verified in `AITask.js` which returns the task object with a `result` property of type Object) to render `JSON.stringify(execRes.data.data.result)` or a default message indicating execution success. Check file to verify.
14. **Build and test**:
   - Run `cd mobile-app-native && npm install`.
   - Run `cd mobile-app-native && npm test` to ensure basic tests pass.
   - Run `cd mobile-app-native/android && ./gradlew clean assembleRelease --no-daemon` to build the APK.
15. **Pre-commit**: Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
16. **Submit**: Submit the branch with proper commit.
